# PrintFlow Website Package

Run the backend with `npm install` and `npm start`. The owner dashboard reads live print-agent state from `/api/auth/agent-status`, displays all owner print work, and communicates with the Windows agent through `/api/print-agent`.
